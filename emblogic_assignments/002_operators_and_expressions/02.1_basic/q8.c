
#include<stdio.h>

int main()
{
	float a=3.3,b=2.2,c=3.3f,d=2.2f,e,f;


	printf("a=%f,b=%f are multipled and result is %f ; with deccimal precision of 3 :%3f\n",a,b,a*b,a*b);

	printf("c=%f,d=%f are multipled and result is %f ; with deccimal precision of 3 :%3f\n",c,d,c*d,c*d);

	printf("enter the float values for e and f\n");
	scanf(" %f%f",&e,&f);

	printf("e=%f,f=%f are multipled and result is %f ; with deccimal precision of 3 :%3f\n",e,f,e*f,e*f);
		
printf(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>\n");


/***************************************************************************************************************************/

	printf("a=%f,b=%f are multipled and result is %f ; with deccimal precision of 3.0 :%3.0f\n",a,b,a*b,a*b);

	printf("c=%f,d=%f are multipled and result is %f ; with deccimal precision of 3.0 :%3.0f\n",c,d,c*d,c*d);

	printf("enter the float values for e and f\n");
	scanf(" %f%f",&e,&f);

	printf("e=%f,f=%f are multipled and result is %f ; with deccimal precision of 3.0 :%3.0f\n",e,f,e*f,e*f);
	

printf(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>\n");

/***************************************************************************************************************************/


	printf("a=%f,b=%f are multipled and result is %f ; with deccimal precision of 3.1 :%3.1f\n",a,b,a*b,a*b);

	printf("c=%f,d=%f are multipled and result is %f ; with deccimal precision of 3.1 :%3.1f\n",c,d,c*d,c*d);

	printf("enter the float values for e and f\n");
	scanf(" %f%f",&e,&f);

	printf("e=%f,f=%f are multipled and result is %f ; with deccimal precision of 3.1 :%3.1f\n",e,f,e*f,e*f);


/***************************************************************************************************************************/
		

printf(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>\n");
	printf("a=%f,b=%f are multipled and result is %f ; with deccimal precision of .1 :%.1f\n",a,b,a*b,a*b);

	printf("c=%f,d=%f are multipled and result is %f ; with deccimal precision of .1 :%.1f\n",c,d,c*d,c*d);

	printf("enter the float values for e and f\n");
	scanf(" %f%f",&e,&f);

	printf("e=%f,f=%f are multipled and result is %f ; with deccimal precision of .1 :%.1f\n",e,f,e*f,e*f);

/***************************************************************************************************************************/
		
printf(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>\n");
	printf("a=%f,b=%f are multipled and result is %f ; with deccimal precision of 0.1 :%0.1f\n",a,b,a*b,a*b);

	printf("c=%f,d=%f are multipled and result is %f ; with deccimal precision of 0.1 :%0.1f\n",c,d,c*d,c*d);

	printf("enter the float values for e and f\n");
	scanf(" %f%f",&e,&f);

	printf("e=%f,f=%f are multipled and result is %f ; with deccimal precision of 0.1 :%0.1f\n",e,f,e*f,e*f);

printf(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>\n");
return 0;
}
